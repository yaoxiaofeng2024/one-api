GateRouter参考
https://www.gaterouter.ai/zh/docs




核心卖点：
1、智能路由：
    第一版是规则路由
    第二版是规则+算法路由
2、USDT支付
3、比较gaterouter，增加了国内的便宜大模型

包装路径：
1、大模型网关基础实现（openai协议、authropic协议）
2、支付购买积分
3、计算token扣费
4、规则路由+智能路由
    - **规则路由**（Level 1）：关键词匹配 → 规则表 → 选模型
        - 示例：`"code|编程|函数|bug"` → deepseek-coder，`"数学|计算|推理"` → o3-mini，`default` → gpt-4o
        - 优点：简单可靠，零延迟开销
        - 缺点：规则写死，无法泛化，关键词匹配太粗糙

    - **意图分类器**（Level 2）：识别任务类型 → 查模型能力矩阵 → 选最优
        - 任务分类体系：代码生成、代码调试、数学推理、创意写作、知识问答、翻译、摘要、闲聊
        - 模型能力矩阵：每项 1-5 分评分（代码/推理/写作/翻译等维度）+ 价格
        - 选模型策略：cheapest（及格中最便宜）、best（最强不看价格）、balanced（性价比 score/price 最高）
        - 分类器实现（注意成本控制，不能把完整 prompt 传给分类模型，否则等于双倍付费）：
            - ✅ 方案一：规则优先 + 本地小模型兜底（推荐，零外部调用成本）
                - 先用规则（关键词/正则）匹配 80% 的明确场景，剩余 20% 交给本地 BERT 级分类器
                - 本地分类器只需提取 prompt 的关键特征（前 200 字 + 关键词），不需要传完整内容
            - ✅ 方案二：Embedding 相似度匹配（极低成本）
                - 预计算每个任务类别的 embedding 向量（如"代码生成"的典型描述）
                - 用户请求来了，只算其 embedding，余弦相似度匹配最近类别
                - 可用本地 Embedding 模型（如 bge-small），零 API 调用成本
            - ⚠️ 方案三：截断 prompt + 外部小模型（有成本但可控）
                - 只传 prompt 前 200 字给 gpt-4o-mini 做分类，输出仅 20-30 tokens
                - 成本约为原请求的 1/10~1/20，但仍有额外开销

    - **复杂度评估**（Level 3）：同类任务区分难易程度，简单任务用便宜模型，复杂任务用强力模型
        - 评估维度：prompt 长度、是否要求推理、是否多步骤、输出期望长度、是否专业领域
        - 实现方式：本地规则计算，不需要额外模型调用
            - prompt 长度 < 50 字 → 低复杂度，> 500 字 → 高复杂度
            - 含"证明/分析/比较/详细" → 加复杂度分
            - 含"一句话/简单/快速" → 减复杂度分
            - 最终评分 1-5，与 Level 2 的分类结果合并决策
        - 路由策略：complexity≤2 → 便宜模型，complexity≥4 → 强力模型，中间 → 性价比模型

    - **实时感知+动态决策**（Level 4）：加入运行时指标，路由决策随环境变化
        - 维护指标：平均延迟、错误率、吞吐量、可用性、剩余配额
        - 决策逻辑：排除不可用 → 排除高错误率 → 综合打分（能力40% + 价格30% + 延迟20% + 可靠性10%）

    - **反馈学习**（Level 5）：闭环优化，用实际结果改进路由决策
        - 隐式反馈：用户重新发同一请求但换了模型 → 上次不满意
        - 显式反馈：UI 上加 👍👎 按钮
        - A/B 测试：同类请求随机分配不同模型，统计满意度
        - 持续更新模型能力评分（移动平均，学习率 0.1）
5、整体需要完成闭环
6、需要提供文档（完整接入流程）
7、涉及相关表，记录核心数据













